<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aanandam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.doctor_journal_api"
        android:allowBackup="true"
        android:icon="@drawable/web_hi_res_512"
        android:label="@string/app_name"
        android:roundIcon="@drawable/web_hi_res_512"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".doctor_info"></activity>
        <activity android:name=".doctor_list" />
        <activity android:name=".doctor_profile" />
        <activity android:name=".patientcreate_account" />
        <activity android:name=".doctorcreate_account" />
        <activity android:name=".patientlogin" />
        <activity android:name=".Doctor_login" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppThemeformain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>